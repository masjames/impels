package app.impels.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 sans typography (no serif). DeepSeek may refine weights per spec §12.
val AppTypography = Typography()
